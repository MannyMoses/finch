import React, { Component } from 'react';

class List extends Component {
    render() {
        return (
            <div className="list">
                {
                    // Map through responses and return in a <Card /> Component
                }
            </div>
        )
    }
};

export default List;
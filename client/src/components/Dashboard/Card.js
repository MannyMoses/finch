import React, { Component } from 'react';

class Card extends Component {
    // declare repsonses as a variable
    // set state with repsonses
    render() {
        return (
            <div className="cardDiv">
                <div className="card">
                    {
                        // Receive responses as props and return in a card div
                    }
                </div>
            </div>
        )
    }
}

export default Card;
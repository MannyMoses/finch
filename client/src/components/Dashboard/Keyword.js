import React, { Component } from 'react';

class Keyword extends Component {
    render() {
        return (
            <div className="Keyword">
                {
                    // Pass down searched word as prop and return as a component
                }
            </div>
        )
    }
};

export default Keyword;